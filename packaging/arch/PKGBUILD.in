pkgname=cockpit-ansible
pkgver=VERSION
pkgrel=1
pkgdesc='Cockpit Ansible Module'
arch=('x86_64')
url='https://github.com/openSUSE/cockpit-ansible'
license=(LGPL)
source=("SOURCE")
sha256sums=('SKIP')

package() {
  depends=(cockpit)
  cd $pkgname
  make DESTDIR="$pkgdir" install PREFIX=/usr
}
